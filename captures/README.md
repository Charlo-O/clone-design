# captures

Browser-derived source material used to generate the cases in `design-md/`.

Supported shapes:

- single page: `captures/<site>/clone.html`
- multi-page session: `captures/<site>/<page>/clone.html`

For logged-in products, keep one browser session alive and save each selected page or state in its own folder.

Current captures:

- [jimeng](./jimeng/)
